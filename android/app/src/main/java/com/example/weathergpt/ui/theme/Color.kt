package com.example.weathergpt.ui.theme

import androidx.compose.ui.graphics.Color

/* ============================================================
   WEATHERGPT HIGH-TECH AI COLOR SYSTEM
   ============================================================ */

/* Background */
val BackgroundDark =
    Color(0xFF080C14)

val BackgroundDeep =
    Color(0xFF05080E)

val BackgroundElevated =
    Color(0xFF0D1424)

/* Surfaces */
val SurfaceDark =
    Color(0xFF101726)

val SurfaceElevated =
    Color(0xFF151E32)

val SurfaceGlass =
    Color(0xFF121A2C)

val SurfaceGlassStrong =
    Color(0xF0101726)

/* Primary AI colors */
val NeonBlue =
    Color(0xFF388BFF)

val ElectricBlue =
    Color(0xFF2563EB)

val NeonCyan =
    Color(0xFF38BDF8)

val AIViolet =
    Color(0xFF7C3AED)

val NeonPurple =
    Color(0xFF8B5CF6)

/*
 * Backward-compatible names used by existing screens.
 * ChatScreen.kt and other older UI files can continue
 * using WeatherBlue / WeatherPurple.
 */
val WeatherBlue =
    NeonBlue

val WeatherPurple =
    AIViolet

/* Secondary glow colors */
val CyanGlow =
    Color(0xFF55F1E8)

val BlueGlow =
    Color(0xFF72B8FF)

val PurpleGlow =
    Color(0xFF9B7BFF)

/* Status */
val SuccessGreen =
    Color(0xFF49E6A5)

val RiskOrange =
    Color(0xFFFFB347)

val RiskRed =
    Color(0xFFFF5F78)

val WarningYellow =
    Color(0xFFFFD166)

/* Text */
val TextPrimary =
    Color(0xFFF5F8FF)

val TextSecondary =
    Color(0xFFB8C3D6)

val TextMuted =
    Color(0xFF7D8BA3)

val TextDim =
    Color(0xFF566176)

/* Borders */
val BorderBlue =
    Color(0x664E9EFF)

val BorderPurple =
    Color(0x668F6CFF)

val BorderSubtle =
    Color(0x263A4A69)

/* Weather */
val RainBlue =
    Color(0xFF55B9FF)

val CloudBlue =
    Color(0xFF7E9CC8)

val TemperatureOrange =
    Color(0xFFFF8D5C)

/* Map */
val MapAccent =
    Color(0xFF58A6FF)

val FloodAccent =
    Color(0xFF36D1DC)

val EarthquakeAccent =
    Color(0xFFFF718B)

val DamAccent =
    Color(0xFFB78CFF)
